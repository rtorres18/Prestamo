<%= form_for(@libro) do |f| %>
  <% if @libro.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@libro.errors.count, "error") %> prohibited this libro from being saved:</h2>

      <ul>
      <% @libro.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label "Titulo del libro" %><br />
    <%= f.text_field :titulo %>
  </div>
  <div class="field">
    <%= f.label "Fecha del prestamo" %><br />
    <%= f.date_select :fechaprestamo %>
  </div>
  <div class="field">
    <%= f.label "Nombre de la persona" %><br />   
    <%= f.collection_select :usuario_id, Usuario.all, :id, :nombre, :prompt => true %>

  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
