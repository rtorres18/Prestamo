<h1>Listing libros</h1>

<table>
  <tr>
    <th>Titulo</th>
    <th>Fechaprestamo</th>
    <th>Cantidaddias</th>
    <th>Usuario</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @libros.each do |libro| %>
  <tr>
    <td><%= libro.titulo %></td>
    <td><%= libro.fechaprestamo %></td>
    <td><%= libro.cantidaddias %></td>
    <td><%= libro.usuario.nombre %></td>
    <td><%= link_to 'Show', libro %></td>
    <td><%= link_to 'Edit', edit_libro_path(libro) %></td>
    <td><%= link_to 'Destroy', libro, confirm: 'Are you sure?', method: :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Libro', new_libro_path %>
